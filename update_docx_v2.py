#!/usr/bin/env python3
"""直接修改第2版 docx：统一"3D框中心回归"说法、压缩字数、统一格式"""
from docx import Document

doc = Document('work/基于无锚框 3D 目标检测的仓储机器人实时语义感知2.docx')
paras = doc.paragraphs

def replace_para_text(para, new_text):
    """替换整个段落的文本，保留首个run格式"""
    for run in para.runs:
        run.text = ''
    if para.runs:
        para.runs[0].text = new_text

# ---------- P2: 原理首段 - 压缩 ----------
if len(paras) > 2:
    p = paras[2]
    replace_para_text(p,
        "在移动机器人的工业仓储应用中，环境感知是决策与路径规划的基础。"
        "传统基于锚框（Anchor-based）的 3D 检测方法（如 PointPillars、SECOND）"
        "依赖预设几何锚框进行偏移回归，"
        "在非结构化仓储场景中因目标尺寸与姿态高度异构而面临适配失配问题，"
        "导致定位精度下降与类别扩展困难。"
        "工业仓储中多种规格的货架与非标工作台具有高度形态多样性，"
        "其几何形状、朝向不符预设锚框分布，"
        "当目标尺度超出训练统计分布时，基于锚框的匹配机制会因 IoU 损失失效而产生严重截断。"
        "传统方法需手动调整锚框模板以适配不同设备，本质上缺乏对新设施的适应能力。"
    )
    print("P2 已修改")

# ---------- P5: 图标题 ----------
if len(paras) > 5:
    p = paras[5]
    replace_para_text(p, "图X 基于3D框中心回归的3D语义感知推理")
    print("P5 已修改")

# ---------- P7: 第一性原理段落 - 压缩 ----------
if len(paras) > 7:
    p = paras[7]
    replace_para_text(p,
        "本文所依托的无锚框（Anchor-free）感知原理，核心在于将 3D 检测建模为"
        "\u201c3D框中心回归\u201d问题，而非\u201c候选框匹配\u201d问题。"
        "任何具有物理维度的物体在 3D 空间中均拥有唯一的 3D 包围盒中心。"
        "基于 CenterPoint 架构，模型通过在鸟瞰图（BEV）特征图上直接预测目标中心点热力图，"
        "网络输出的每一个峰值天然对应一个物体的中心位置，"
        "将 3D 检测简化为关键点定位与属性回归两个子任务。"
        "这种设计摒弃了启发式的几何限制，使网络专注提取点云中的语义特征。"
    )
    print("P7 已修改")

# ---------- P20: 泛化性解耦 - 压缩 + 修复 typo ----------
if len(paras) > 20:
    p = paras[20]
    replace_para_text(p,
        "该原理的泛化性优势表现为几何属性与类别特征的解耦：网络通过多任务头部"
        "直接回归物体的长、宽、高及方位角，无需修改预设几何约束即可自适应输出任意尺寸的包围盒。"
        "对于仓储场景中新增的异形设备，仅需少量样本微调即可习得其中心点分布规律，"
        "实现对非标物体的精准感知。在路径规划任务中，3D框中心回归方式确保输出边界框"
        "平滑稳定，避免传统方法因锚框匹配不稳定造成的预测跳变。"
    )
    print("P20 已修改")

# ---------- P29: 方法首段 - 压缩 ----------
if len(paras) > 29:
    p = paras[29]
    replace_para_text(p,
        "本工作采用混合通用 LiDAR 检测数据集与自采仓储专用数据集的联合训练策略。"
        "利用公开大规模仓储 LiDAR 点云数据集作为泛化性先验，涵盖不同仓库布局与传感器配置下的"
        "多视角扫描帧，提供丰富的几何多样性。训练采用两阶段范式：第一阶段在公开数据集上全量预训练，"
        "使 Voxel Feature Encoder 和 3D Backbone 学习跨场景的通用几何模式；"
        "第二阶段在自采数据上全参数微调。Anchor-Free 架构使定位与分类能力天然解耦——"
        "公开数据集增强定位子网络鲁棒性，自采数据集定向注入类别知识。"
        "实验表明，仅使用自采数据训练时叉车类别的 3D AP 为 0.0%，"
        "联合预训练加微调后提升至 61.9%（Epoch 30, CenterPoint++），"
        "验证了混合训练策略对稀疏类别的显著增益。"
    )
    print("P29 已修改")

# ---------- P31: 度量学习段落 - 压缩 ----------
if len(paras) > 31:
    p = paras[31]
    replace_para_text(p,
        "当仓储环境引入新型设备时，传统方法需重标大量数据并全量重训，部署成本高。"
        "本文提出基于度量学习的轻量级新类别登记机制：利用 Center Head 共享卷积层输出的"
        "64 维特征向量构建在线类别原型库，新类别仅需 3~5 个标注样本即可注册。"
        "对检测到的目标实例提取中心点位置的特征向量作为嵌入表征，"
        "收集 K=3~5 个样本的嵌入向量计算类别原型（算术平均），"
        "推理时通过余弦相似度最近邻匹配进行补充分类，原型库支持在线增量更新。"
        "相比全量微调或分类头重训练，度量学习方法仅需 K\u22645 个样本，"
        "不修改网络权重以免疫灾难性遗忘，新类别注册仅需存储一个 64 维向量，部署极其灵活。"
    )
    print("P31 已修改")

# ---------- P35: 收敛段落 - 压缩 ----------
if len(paras) > 35:
    p = paras[35]
    replace_para_text(p,
        "在训练收敛性方面，图 X 展示了模型在完整仓储数据集上的训练损失收敛曲线。"
        "分类损失在前 5 个 Epoch 迅速下降，表明模型快速习得了 BEV 热力图中各类别的中心分布模式；"
        "回归损失持续平滑收敛。Epoch 20 学习率衰减后出现明显二次收敛拐点，"
        "最终在 Epoch 30 达到稳定状态。训练损失与验证损失未出现显著分裂，"
        "表明数据增强策略下模型获得良好泛化能力，未发生过拟合。"
    )
    print("P35 已修改")

# ---------- P39: mAP 说明 - 压缩 ----------
if len(paras) > 39:
    p = paras[39]
    replace_para_text(p,
        "采用 3D 平均精度（3D mAP）作为评测指标。对每对预测框与真实框，"
        "先在 BEV 平面计算旋转矩形 2D IoU，结合高度维度重叠得 3D IoU；"
        "将预测框按置信度降序排列，对每个 GT 框选取 IoU\u2265阈值（0.5/0.7）且置信度最高的"
        "预测框作为 True Positive，未匹配的计为 False Positive，"
        "未被匹配的 GT 框计为 False Negative；"
        "基于 41 个等间距 Recall 采样点插值计算 11 点插值平均精度（AP）。"
        "最终以各类别 3D AP 均值作为 3D mAP，辅以 BEV mAP 和 AOS（平均朝向相似度）作为参考。"
    )
    print("P39 已修改")

# ---------- P44: 推理可视化 - 压缩 ----------
if len(paras) > 44:
    p = paras[44]
    replace_para_text(p,
        "在推理可视化方面，图 X 展示了 CenterPoint 检测系统在仓储场景下的 BEV 与 3D 多视图推理结果。"
        "各类检测框分别对应五类仓储实体，与对应点云簇高度吻合，"
        "验证了3D框中心回归范式的定位精度。在密集货架排列场景中，"
        "相邻目标框边界清晰无粘连，体现了该方法在多目标场景下的实例区分能力。"
        "从 3D 视角观察，检测框的尺寸回归与朝向角估计均保持较高几何一致性。"
    )
    print("P44 已修改")

# ---------- P48: 泛化性评估 - 压缩 ----------
if len(paras) > 48:
    p = paras[48]
    replace_para_text(p,
        "在泛化性评估中，本系统表现出良好的场景自适应性。"
        "跨布局迁移实验表明，模型在未见过的异形货架环境下仍保持高召回率，"
        "优于传统 Anchor-based 方法。"
        "Anchor-Free 架构面对设备迭代或布局重构时无需重新校准参数，"
        "且新增类别仅需对 Head 层进行极少样本微调即可迅速习得。"
    )
    print("P48 已修改")

# ---------- P50: 实时性能 - 压缩 ----------
if len(paras) > 50:
    p = paras[50]
    replace_para_text(p,
        "实时性能方面，在 RTX 4090 计算平台上，感知流水线单帧推理平均延迟 24.0 ms，"
        "帧率达 41.7 FPS。该速度涵盖从点云体素化到前向计算的全流程，"
        "远低于 50 ms 的实时处理窗口要求，确保机器人在高速运行中仍能实时刷新环境感知，"
        "为后续路径规划提供高效且高精度的语义输入。"
    )
    print("P50 已修改")

# ---------- P52: 结尾段 - 改说法 ----------
if len(paras) > 52:
    p = paras[52]
    replace_para_text(p,
        "在导航验证环节，通过对比语义感知输出代价图与传统栅格地图，"
        "重点评估了语义信息对路径生成的辅助价值。实验表明，基于语义的代价图"
        "能够精确识别货架边缘，引导规划算法生成更平滑、安全的避障弧线，"
        "避免传统方法因边界不准导致的急转弯或碰撞风险。"
        "实验结果证明，该感知范式能显著改善现有导航系统的寻径距离与规划成功率。"
        "综上所述，以\u201c3D框中心回归\u201d为核心的 Anchor-free 感知范式，"
        "为复杂仓储环境下实现全局最优路径规划提供了可靠先验信息的高效方案。"
    )
    print("P52 已修改")

# 保存
output_path = 'work/基于无锚框 3D 目标检测的仓储机器人实时语义感知2.docx'
doc.save(output_path)
print(f"\n已保存至: {output_path}")
print("完成！")